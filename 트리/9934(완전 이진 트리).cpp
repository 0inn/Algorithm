#include <iostream>
#include <cmath>
using namespace std;

int k, root;

int main() {
    cin >> k;
    root = pow(2, k) / 2;
    for (int i = 0, num; i < k; i++) {
        cin >> num;
    }
    
    return 0;
}
