#include<bits/stdc++.h>
#define MAX 501
using namespace std;

int n;

// 이해 절대 불가 ,,
int main() {
	ios_base::sync_with_stdio(0);
	cin.tie(0);

	cin >> n;
	for (int i = 0,r,c; i < n; i++) {
		cin >> r >> c;
	}

	return 0;
}